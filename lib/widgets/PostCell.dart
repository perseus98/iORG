import 'package:flutter/material.dart';
import 'package:iorg_flutter/models/PostModel.dart';

Container postCell(PostModel _post) {
  return Container(
    margin: EdgeInsets.all(2.0),
    padding: EdgeInsets.all(2.0),
  );
}
